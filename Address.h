#ifndef ADDRESS_H
#define ADDRESS_H

#include <string>

struct Address {    
    std::string address;
    std::string city;
    std::string state;
    std::string pincode;
    std::string country;
};

#endif
